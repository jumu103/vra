def handler(context, inputs):
    
    old_name = inputs["old_name"]
    new_name = inputs["new_name"]
    
    print("Got two values {0} --and-- {1}" .format(old_name, new_name))
    
    outputs = {}

    return outputs
